# PostgreSQL

The local database is defined by the root `compose.yaml`. Persist schema changes as versioned SQL files in `backend/src/main/resources/db/migration`; do not place ad-hoc SQL in this directory.
