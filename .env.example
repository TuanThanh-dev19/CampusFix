# SQL Server 2022 Developer used by compose.yaml and Spring Boot.
# Local development value only; use a separate secret in deployed environments.
ACCEPT_EULA=Y
MSSQL_PID=Developer
MSSQL_DATABASE=campusfix
MSSQL_SA_PASSWORD=CampusFix_SA_2026!
MSSQL_PORT=1433

DB_URL=jdbc:sqlserver://localhost:1433;databaseName=campusfix;encrypt=true;trustServerCertificate=true
DB_USERNAME=sa
DB_PASSWORD=CampusFix_SA_2026!

# Local-only JWT key. Replace it in every deployed environment.
JWT_SECRET=change-this-local-development-secret-32bytes

# Frontend
VITE_API_BASE_URL=/api/v1
