package com.nexora.category.entity;

/** Canonical dynamic field types supported by category forms. */
public enum DynamicFieldType {
    TEXT,
    TEXTAREA,
    NUMBER,
    SELECT,
    MULTI_SELECT,
    DATE,
    DATETIME,
    BOOLEAN,
    IMAGE
}
